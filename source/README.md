# Source code snapshot

This directory is a code-only snapshot copied on 2026-09-06 from `E:\mcop\mcop` and the top-level helper scripts in `E:\chatgpt`. It preserves the earlier analysis, single-cell, environmental, manuscript, docking and MD scripts for review and historical reproducibility.

Virtual environments, `site-packages`, runtime bundles, raw data, caches, generated trajectories and checkpoints are excluded. The exact file list and SHA-256 hashes are in `code_snapshot_manifest.json`.

The maintained project entry points remain under `work/` and `work/scripts/`; this snapshot keeps the earlier source tree available separately.
