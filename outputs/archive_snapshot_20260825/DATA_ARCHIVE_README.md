# MCOP–CRC archive snapshot (2026-08-25)

This directory records the GitHub-safe portion of the complete local project archive at `D:\mcop and CRC`.

Included in GitHub:

- complete archive inventory and audit;
- manuscript and local figure exports;
- Phase 2G analysis outputs saved before the local H5AD rerun;
- literature-audit records;
- Stage 1–3 Census query manifests and `stage3_summary.json`;
- figure-generation and redesign scripts.

The large raw-expression artifacts and runtime cache remain in the verified local archive and are not duplicated in Git history. Their paths, byte sizes, and SHA-256 checksums are recorded in `RAW_DATA_SHA256.csv` and `ARCHIVE_FILE_INVENTORY.csv`. The principal CELLxGENE H5AD is 3,759,324,000 bytes, which is above the 2 GB per-file Git LFS limit on GitHub Free/Pro accounts. The local audit confirms that the archived H5AD matches its source byte-for-byte by SHA-256.

Local archive root: `D:\mcop and CRC`

Frozen project branch at archive creation: `phase2f-compartment-external-replication`

Archive-creation commit: `b0aa8c5774401d2238b70e3ec232f05ab6be9ed8`

The repository was subsequently fast-forwarded to the existing remote manuscript-freeze commits before publishing this GitHub snapshot.
